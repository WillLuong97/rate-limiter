#include <string>
#include <iostream>
#include <map>
#include <BackendConfig.h>

void 
BackendConfig::
AddOrUpdateToBackendConfigMap(string backend_id, map<string, string> backend_config) {
    //Todo: implement the logic to add into the local data structure     
}

pair<string, string>
BackendConfig::GetBackendConfig(string backend_id) { 

}

void 
BackendConfig::RemoveBackendConfig(string backend_id) {

}
